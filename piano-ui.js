const BLACK=new Set(['C#','D#','F#','G#','A#']);
export function buildKeyboard(el,minMidi=36,maxMidi=84){const names=['C','C#','D','D#','E','F','F#','G','G#','A','A#','B'];for(let midi=minMidi;midi<=maxMidi;midi++){const name=names[midi%12];const octave=Math.floor(midi/12)-1;const pitch=`${name}${octave}`;const key=document.createElement('div');key.className=`key${BLACK.has(name)?' black':''}`;key.dataset.pitch=pitch;key.textContent=name==='C'?pitch:'';el.appendChild(key)}}
export function setKeyActive(el,pitch,hand,active){const key=el.querySelector(`[data-pitch="${CSS.escape(pitch)}"]`);if(key)key.classList.toggle(`active-${hand}`,active)}
export function drawRoll(canvas,composition){const dpr=window.devicePixelRatio||1;const rect=canvas.getBoundingClientRect();canvas.width=Math.max(1,rect.width*dpr);canvas.height=180*dpr;const ctx=canvas.getContext('2d');ctx.scale(dpr,dpr);const w=rect.width,h=180;ctx.clearRect(0,0,w,h);ctx.strokeStyle='#252a33';ctx.lineWidth=1;for(let b=0;b<=composition.totalBeats;b+=4){const x=b/composition.totalBeats*w;ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,h);ctx.stroke()}const pitchNum=p=>{const m=p.match(/^([A-G]#?)(\d)$/);const map={C:0,'C#':1,D:2,'D#':3,E:4,F:5,'F#':6,G:7,'G#':8,A:9,'A#':10,B:11};return (Number(m[2])+1)*12+map[m[1]]};for(const n of composition.notes){const x=n.start/composition.totalBeats*w;const width=Math.max(2,n.duration/composition.totalBeats*w);const y=h-(pitchNum(n.pitch)-35)/51*h;ctx.fillStyle=n.hand==='right'?'#6ba9df':'#d69a6d';ctx.fillRect(x,Math.max(2,y),width,3)}}
